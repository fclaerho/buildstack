A build stack wrapper.

INSTALLATION

	$ pip install -i https://pypi.fclaerhout.fr/simple/ code

or, if that repository is not available:

	$ git clone $this
	$ python setup.py install

DEVELOPMENT

	$ python setup.py develop